<link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.9"></script>


<style type="text/css">
.btn {
  border-radius: 5px;
    border: 2px solid black;
    background-color: white;
    color: black;
    padding: 14px 28px;
    font-size: 16px;
    cursor: pointer;
}
.danger {
    border-color: #F012BE;
    color: #F012BE;
}

.danger:hover {
    background: #F012BE;
    color: white;
}




.tm-btn-white-big1 {
    font-size: 1.5rem;
    font-family: 'Roboto';
   
    padding: 0.5rem 5rem;
    border-radius: 0.5rem;
}

  a {
  color: black;
  text-decoration: none;  no underline 
}

a:hover {text-decoration: none;
    color: #F012BE;
}



	h1 { color: #111; font-family: 'Open+Sans', sans-serif; font-size: 275px; font-weight: bold; letter-spacing: -1px; line-height: 1; text-align: center; }

</style>

<body onunload="">


<%=stylesheet_link_tag "stat"%>
<div id="typed-strings">
    <p>Stats Portal.</p>
</div>


<p><h1><%= link_to "", gen_stats_path  , id:"typed"%></h1></p>
<script>
    var options = {
        typeSpeed: 150,
          showCursor: true,

        stringsElement: '#typed-strings'
    }
    var typed = new Typed("#typed", options);
window.addEventListener('popstate', () => {
  location.reload();
}, false);





</script>







<div class="container">

  <input type="text" id="contact_name" name="contact_name" placeholder="Search..." >
  <div class="search"></div>
</div>
<center>
&nbsp;&nbsp;

<%= link_to 'View All Crimes', crimes_path,{ :class=>"tm-btn-white-big1 btn danger" }  %>&nbsp;&nbsp;
<%= link_to 'Back to Dock', slider_path,{ :class=>"tm-btn-white-big1 btn danger" }  %></center>


<script type="text/javascript">
	
  $(function() {
    var availableTags = <%= raw json_escape(@loc.to_json) %>;
    $( "#contact_name" ).autocomplete({
      source: availableTags
    });
  } );




$(document).keypress(function (e) {
	                console.log("aman");
if($( '#contact_name' ).val().length==1)
	return false;
             var code = e.keyCode ? e.keyCode : e.which;
             if (code.toString() == 13) {
                console.log("aman");
                QLSearch(); // call the function to redirect to another page
                 return false;
             }
     });

// Function that redirect the user to another page
function QLSearch() {
          window.location = '/stats/graphs/'+$( '#contact_name' ).val();
}


</script>